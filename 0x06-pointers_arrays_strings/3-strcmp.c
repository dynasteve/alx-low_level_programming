#include "main.h"
#include <string.h>

/**
 * _strcmp - Conactinates two strings
 * @s1: character value.
 * @s2: character value
 * Return: returns pointer of @dest
 */

int _strcmp(char *s1, char *s2)
{
int result = strcmp(s1, s2);
return (result);
}
